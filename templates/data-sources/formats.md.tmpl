---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "{{.Name}} {{.Type}} - {{.RenderedProviderName}}"
subcategory: ""
description: |-
{{ .Description | plainmarkdown | trimspace | prefixlines "  " }}
---

# {{.Name}} ({{.Type}})

{{ .Description | trimspace }}

## Example Usage

### Basic Usage (All Formats)

```terraform
data "bmlt_formats" "all" {}

# Access format information
output "format_count" {
  value = length(data.bmlt_formats.all.formats)
}

output "format_names" {
  value = [for format in data.bmlt_formats.all.formats :
    format.translations[0].name if length(format.translations) > 0
  ]
}
```

### Language-Filtered Formats with Key Map

```terraform
# Get formats filtered by language with convenient key-based access
data "bmlt_formats" "english" {
  language = "en"
}

# Use formats in a meeting resource
resource "bmlt_meeting" "example" {
  name       = "My Meeting"
  format_ids = [
    data.bmlt_formats.english.formats_by_key["O"].id,   # Open
    data.bmlt_formats.english.formats_by_key["D"].id,   # Discussion
    data.bmlt_formats.english.formats_by_key["BT"].id,  # Basic Text
  ]
  # ... other meeting attributes
}

# Access individual format details
output "discussion_format" {
  value = {
    name        = data.bmlt_formats.english.formats_by_key["D"].name
    description = data.bmlt_formats.english.formats_by_key["D"].description
  }
}

# Get all available format keys for this language
output "available_format_keys" {
  value = keys(data.bmlt_formats.english.formats_by_key)
}
```

### Multi-Language Support

```terraform
# Get formats for multiple languages
data "bmlt_formats" "english" {
  language = "en"
}

data "bmlt_formats" "spanish" {
  language = "es"
}

# Compare format names across languages
output "format_comparison" {
  value = {
    english_discussion = data.bmlt_formats.english.formats_by_key["D"].name
    spanish_discussion = data.bmlt_formats.spanish.formats_by_key["D"].name
  }
}
```

{{ .SchemaMarkdown | trimspace }}
